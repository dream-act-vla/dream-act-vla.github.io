title: "DreamActVLA"
description: "Accurate Asynchronous Inference for Vision-Language-Action Models"
baseurl: "" # leave empty for org site (dream-act-vla.github.io)
url: "" # your GitHub Pages URL

# Build settings
markdown: kramdown
kramdown:
  math_engine: null # we handle math client-side with KaTeX
  input: GCM
  toc_levels: "1..3"

# Exclude from build
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
